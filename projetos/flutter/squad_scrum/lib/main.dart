import 'package:flutter/material.dart';
import 'package:squad_scrum/Screen/login.dart';

void main() {
  runApp(
    const MaterialApp(
      debugShowCheckedModeBanner: false,
      home: Login(),
    ),
  );
}